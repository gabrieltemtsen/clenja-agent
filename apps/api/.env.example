PORT=8787
THIRDWEB_SECRET_KEY=
X402_SERVER_WALLET=0x0000000000000000000000000000000000000000
X402_NETWORK=celo

WALLET_PROVIDER=para
# legacy|goat (goat routes execution through GoatWalletProvider adapter)
EXECUTION_ENGINE=goat

PARA_MODE=mock
# Para server SDK auth key (required in live mode)
PARA_API_KEY=
# Optional legacy HTTP base (unused by server-sdk integration)
PARA_API_BASE=
# Optional custom Celo RPC for getWalletBalance
PARA_CELO_RPC_URL=https://forno.celo.org
PARA_TIMEOUT_MS=12000
PARA_FALLBACK_TO_MOCK_ON_ERROR=true
# Legacy endpoint overrides (unused by server-sdk integration)
PARA_EP_CREATE_OR_LINK=/wallet/create-or-link
PARA_EP_BALANCE=/wallet/balance
PARA_EP_SEND_PREPARE=/wallet/send/prepare
PARA_EP_SEND_EXECUTE=/wallet/send/execute

TURNKEY_ORGANIZATION_ID=
TURNKEY_API_PUBLIC_KEY=
TURNKEY_API_PRIVATE_KEY=
CELO_RPC_URL=https://forno.celo.org

OFFRAMP_MODE=mock
# legacy|clova
OFFRAMP_PROVIDER=legacy
OFFRAMP_API_BASE=
OFFRAMP_API_KEY=
# auto|api_key|x402 (auto prefers API key when available)
OFFRAMP_AUTH_MODE=auto
# Optional backend-to-backend x402 header when testing without API key
OFFRAMP_X402_PAYMENT_HEADER=
OFFRAMP_TIMEOUT_MS=15000
OFFRAMP_FALLBACK_TO_MOCK_ON_ERROR=true
OFFRAMP_EP_QUOTE=/offramp/quote
OFFRAMP_EP_CREATE=/offramp/create
OFFRAMP_EP_STATUS=/offramp/status

# Default beneficiary for live clova cashout (used when no full bank details are supplied)
CASHOUT_DEFAULT_COUNTRY=NG
CASHOUT_DEFAULT_BANK_NAME=
CASHOUT_DEFAULT_ACCOUNT_NAME=
CASHOUT_DEFAULT_ACCOUNT_NUMBER=
CASHOUT_DEFAULT_BANK_CODE=

STRICT_LIVE_MODE=false

STATE_DB_PATH=./.data/state.json

PRICE_WALLET_BALANCE=$0.001
PRICE_WALLET_SEND_PREPARE=$0.002
PRICE_WALLET_SEND_CONFIRM=$0.002
PRICE_OFFRAMP_QUOTE=$0.005
PRICE_OFFRAMP_CREATE=$0.01

OPENAI_API_KEY=
CLENJA_LLM_MODEL=gpt-4o-mini
CLENJA_LLM_INTENT_ENABLED=true
CLENJA_LLM_TIMEOUT_MS=6000
